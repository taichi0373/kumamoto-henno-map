name: Build for backend

# ワークフロー共有設定
on:
  workflow_call:
    inputs:
      timeout-minutes:
        description: 'タイムアウト（分）'
        type: number
        default: 20
      backend-dir:
        description: 'バックエンドディレクトリ'
        type: string
        default: 'apps/back'
      java-distribution:
        description: 'Javaディストリビューション'
        type: string
        default: 'temurin'
      java-version:
        description: 'Javaバージョン'
        type: string
        default: '21'
  
env:
  TZ: Asia/Tokyo
  BACKEND_DIR: ${{ inputs.backend-dir }}

# jobs: 動作内容
jobs:
  # バックエンドのビルド処理
  backend-build:
    name: Backend Build
    timeout-minutes: ${{ inputs.timeout-minutes }}
    # Github-hosted runner を使用
    runs-on: ubuntu-latest

    env:
      GITHUB_USERNAME: ${{ github.actor }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    defaults:
      run:
        working-directory: ${{ env.BACKEND_DIR }}

    # steps: 動作手順
    steps:

      # リポジトリ取得
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          submodules: true

      # Java セットアップ
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: ${{ inputs.java-distribution }}
          java-version: ${{ inputs.java-version }}
          cache: gradle

      # 実行権限
      - name: Make Gradle Wrapper executable
        run: chmod +x ./gradlew

      # ビルド
      - name: Compile Java
        run: ./gradlew compileJava

      # Javadoc 生成
      - name: Generate Javadoc
        run: ./gradlew javadoc

      # Javadoc アップロード
      - name: Upload Javadoc
        uses: actions/upload-artifact@v4
        with:
          name: backend-javadoc
          path: ${{ env.BACKEND_DIR }}/build/docs/javadoc