-- ============================================================
-- 開発・テスト環境専用セットアップスクリプト
-- ============================================================
-- 警告: 本番環境では絶対に実行しないこと
--
-- 【用途】
--   開発・テスト環境で初期管理者ユーザーを投入するためのスクリプト。
--   setup.sql（本番安全）とは分離されており、docker-entrypoint-initdb.d
--   からは自動実行されない（サブディレクトリのため）。
--
-- 【実行方法（手動）】
--   psql -h localhost -U user -d kumamoto_henno_map -f config/database/dev/setup_dev.sql
-- ============================================================

-- 開発用管理者ユーザー（admin / admin）
\ir '../DML/TABLE/USERS.SQL'
